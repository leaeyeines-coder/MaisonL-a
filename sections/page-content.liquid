<section class="page-content">
  <div class="container page-content__inner">
    <h1>{{ page.title }}</h1>
    <div class="rte">{{ page.content }}</div>
  </div>
</section>

{% schema %}
{
  "name": "Contenu de page",
  "settings": [],
  "presets": [{ "name": "Contenu de page" }]
}
{% endschema %}
